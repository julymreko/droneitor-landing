{
  "name": "droneitor-landing",
  "private": true,
  "version": "1.0.0",
  "description": "Droneitor — landing de generación de leads (HTML/CSS/JS plano, sin build step).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
